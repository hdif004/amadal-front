// Prerender configuration disabled
const prerenderConfig = {
    set: () => prerenderConfig,
    blacklisted: () => prerenderConfig,
    whitelisted: () => prerenderConfig
};

export default prerenderConfig;